# macOS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# analysis outputs (regenerable plots)
analysis/output/

# logs
*.log

# Spectra / openspec local state
openspec/.vector-search.db*
.spectra/

# local data (canonical data lives on mini-che NVMe, never in git)
data/
*.parquet
*.duckdb
*.duckdb.wal

# credentials — repo is PUBLIC; these must never be tracked
tdx.json
*.credentials.json
.env
.env.*

# IDD tree-lock (idd-tree-lock.sh #183) — per-machine session state, not tracked
.claude/.idd/tree-lock
